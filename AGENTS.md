# AGENTS.md



本文件是本仓库**唯一的治理文件**，对多种 AI 编码工具（Claude Code 默认读取本文件，其他工具同样适用）与人类贡献者一体生效。前半部分是项目规范：环境、数据流、不变量、读取约定与提交规则；末尾的 **[RULES 规则与标准](#rules-规则与标准)** 章节收编了原四个独立规则文件（教材清洗标准、图表策略、内容权利政策、贡献流程），原文件已删除，历史版本见 Git 记录。



# 项目规范：软考高级系统架构设计师备考资料



本仓库不是应用程序，而是一个**受治理的知识库**：把系统架构设计师考试的大纲、教材（OCR 提取）和历年真题整理为可检索的 Markdown，并用 Python 脚本 + CI 强制一组不变量，保证内容可复核、可追溯、不冒充官方。编辑时真正的难点不在改字，而在**同步维护正文、数据清单、人工索引与校验脚本四者之间的一致性**。



## 内容架构与数据流



仓库按**职责**分四层，而不是按资料堆放。这一点是后续所有工作的前提：



| 层 | 目录 | 职责 | 是否纳入内容门禁 |

|---|---|---|---|

| 正文层 | `content/` | 唯一可读正文，网页展示与 RAG 索引的取数根目录 | 是（全部规则） |

| 来源层 | `sources/` | 第三方原始材料与其书目事实；扫描件本身不入库 | 部分（真题来源目录参与 manifest 一致性） |

| 清单层 | `catalog/` | 结构与数量的唯一真相，机器可读 | 是 |

| 资产层 | `assets/` | 依法取得并逐一对照后落盘的图片与矢量图 | 是（存在时） |

| 旁路 | `verification/` | 证据、出处、裁决与待复核记录 | 否（只查编码与换行） |



三套语料在正文层与来源层的对应关系：



| 语料 | 来源层 | 正文层（唯一可读稿） | 规模 |

|---|---|---|---|

| 大纲 | `sources/00-系统架构设计师考试大纲/`（69 页扫描件，无文本层，不入库） | `content/00-…-清洗版/` | 前言 + 卷首文件材料 + 考试说明 + 3 个考试范围科目 + 题型举例 + INDEX |

| 教材 | `sources/01-系统架构设计师教材/`（720 页扫描件，含 OCR 文本层，不入库） | `content/01-…-清洗版/` | 《系统架构设计师教程》2022 第 2 版，前言 + 20 章（每章一个目录，共 119 节）+ INDEX |

| 真题 | `sources/02-历年真题/` + `sources/02-历年真题(补充)/` | `content/02-历年真题-清洗版/` | 36 份唯一试卷 + 1 份独立备选 + INDEX |



大纲与教材的 OCR 提取稿已从版本库移除：**清洗稿是唯一正文层**，复核时直接对照 `sources/` 下登记的扫描件，不要重建提取稿目录。



治理层把正文与机器清单绑定，校验脚本据此判定一致性：



- `content/INDEX.md` — 正文层根导航，链接三套语料的 `INDEX.md` 与真题总索引。

- `content/02-历年真题总索引.md` — 人工/AI 查询真题的统一入口，规定每卷的“首选文件”。

- `catalog/corpora.json`（schema 1）— 语料总清单：层根、三套语料的 content_root/source_roots、各 `kind` 的期望文档数、真题不变量（36/17/90/53）、源扫描件的字节数与 SHA-256。**数量类断言一律从这里读，不再散落在脚本和正文里。**

- `catalog/exams.json`（schema 4）— 真题 manifest：36 份 canonical、90 份 source_versions 路径、来源目录与同名冲突映射。校验器断言 manifest、两份索引、正文与 frontmatter 的 `id` 彼此一致。



数据流：**正文与清单** →（`validate_knowledge_base.py` 断言四层结构、frontmatter、manifest、两份索引与正文彼此一致，并做编码与结构检查）→ **CI 门禁**。



## 正文文件头标签（frontmatter）



`content/` 下**每个** Markdown 必须以一段扁平 YAML 开头，供网页路由与知识库索引使用。格式刻意受限——**不嵌套、不用列表、每行 `key: value`、字符串加双引号、整数不加**——因为校验器只用标准库解析，不能引 PyYAML：



```yaml

---

id: "textbook-ch07"

corpus: "textbook"

slug: "ch07-architecture-design-fundamentals"

title: "系统架构设计基础知识"

kind: "chapter"

order: 7

source_pages: "248-270"

---

```



| 字段 | 必填范围 | 取值 |

|---|---|---|

| `id` | 全部 | 全仓库唯一；真题的 `id` 必须与 `catalog/exams.json` 的 `id` 集合完全相等 |

| `corpus` | 全部 | `root` / `outline` / `textbook` / `exams` |

| `slug` | 全部 | 纯小写 ASCII，用 `-` 连接；同一语料内唯一；网址由它生成，中文文件名不参与路由 |

| `title` | 全部 | 中文标题，非空 |

| `kind` | 全部 | `root-index` / `index` / `master-index` / `preface` / `chapter` / `section` / `exam` / `exam-variant` |

| `order` | `preface`、`chapter`、`section` | 整数，前言为 0；`section` 为该节在本章内的序号 |

| `parent` | `section` | 所属章的 `id`；节的 `id` 必须是它加 `-sNN` 后缀 |

| `source_pages` | 教材 `chapter`、`section` | 原书印刷页范围，形如 `"248-270"`；节的范围必须落在所属章范围之内 |

| `year` / `session` / `subject` | `exam`、`exam-variant` | 整数年份；`h1`/`h2`；`comprehensive`/`case-analysis`/`essay` |



新增或删除正文文件时，必须同步改 `catalog/corpora.json` 的 `expected_documents`，否则门禁会报数量不符。



`verification/` 是核查记录的旁路目录，与正文物理隔离：正文只放正文，证据/出处/裁决/待复核只写在这里。该目录**不纳入内容门禁**（校验器只查编码与换行），也不要求覆盖率——详见 [verification/README.md](verification/README.md)。



## 命令



环境由 **uv** 管理，解释器由 uv 按 `requires-python = ">=3.13"` 自动选择（优先复用本机已装的 3.13+），依赖锁定在 `uv.lock`。**所有脚本必须经 `uv run` 执行**，不要直接调用系统 `python`/`python3`。本机没有可用解释器时 uv 才会自动下载，并在项目内创建 `.venv/`（已被 `.gitignore` 忽略），无需手动 `venv`/`pip`。



```bash

# 主校验门禁（仅标准库；CI 必跑，本地交付前必跑）

uv run python scripts/validate_knowledge_base.py



# 依赖/锁文件维护

uv lock --check   # 断言 uv.lock 与 pyproject.toml 一致（CI 必跑）

uv lock           # 改了 pyproject.toml 后重新锁定，并提交 uv.lock



# 代码风格（CI 必跑；ruff 固定在 lint 组，默认不装）

uv run --group lint ruff check scripts/

uv run --group lint ruff format scripts/          # CI 用 ruff format --check

```



- 工具链文件：`pyproject.toml`（`requires-python = ">=3.13"`、`[tool.uv] package = false` + `required-version = ">=0.10"`、`[dependency-groups]` 仅 `lint = ["ruff==0.16.4"]`、`[tool.ruff]` 行宽 120）、`uv.lock`（必须入库）。本仓库不是可安装的包，uv 只负责按 `requires-python` 准备解释器与按需依赖。

- `validate_knowledge_base.py` 只依赖标准库，**不要**给它加第三方依赖；`lint` 是非默认组（`--group` 按需同步），因此主门禁在没有任何第三方包的环境里也必须通过。

- ruff 配置刻意不启用 `E501`（中文字符串无法拆行）和 `RUF001/002/003`（全角标点对中文项目是纯误报，会触发 305 次）。

- CI（`.github/workflows/knowledge-base-quality.yml`）检出后用 `astral-sh/setup-uv`（按 SHA 固定）装 uv，再跑 `uv lock --check` + 主门禁 + `py_compile` + `ruff check` + `ruff format --check`；`uv run --locked` 保证 CI 不会偷偷改锁文件。

- 无测试框架：`validate_knowledge_base.py` 是单文件顺序执行的 `Validator` 类，校验器本身即“测试”。改脚本后整跑即可，没有单测可单独运行。



## 关键不变量（改内容必须同步更新）



数量类断言统一存放在 `catalog/corpora.json`，校验器从那里读取，不再硬编码。改动任何正文/题目时，必须让下列约束在**清单 JSON + frontmatter + 索引 + 正文**里同时成立，否则校验必红：



- 四层结构：`content/INDEX.md`、`sources/README.md`、`catalog/README.md`、`assets/README.md` 必须存在；`sources/00-…`、`sources/01-…` 各须有 README 说明期望的本地文件；`.gitignore` 必须保留 `*.pdf` 规则。

- `catalog/corpora.json`（schema 1）：`layers` 中每个路径必须是真实目录；`root_index.path` 必须是 `content/INDEX.md`；`corpora` 必须恰好是 `outline`/`textbook`/`exams`；每个 `sha256` 必须是 64 位小写十六进制**且同时出现在 `DATA_SOURCES.md`**（防两处漂移）；扫描件不得标 `committed: true`。

- frontmatter：`content/` 下每个 md 都要有；必填字段齐全；`id` 全仓库唯一；`slug` 语料内唯一且为小写 ASCII；按 `(corpus, kind)` 统计的文档数必须等于 `expected_documents`；`kind=exam` 的 `id` 集合必须等于 `catalog/exams.json` 的 `id` 集合。

- 教材章节：章目录名须匹配 `第XX章-标题`，节文件名须匹配 `第XX节-标题.md`；每个 `kind: "section"` 的 `parent` 必须是某个真实存在的章 `id`，`id` 必须是 `<章id>-sNN`，`source_pages` 必须落在所属章的范围内。

- 真题：36 份 canonical、17 组同名冲突、90 份 source_versions、53 份来源正文（值取自 `corpora.json` 的 `invariants`）；每份 source_version 的 `path` 必须真实存在、落在 `source_catalog` 对应根目录下，且与该卷的 `preferred`/`alternatives` 完全对应；canonical 集合必须与两个来源目录中的试卷文件一一对应，无遗漏、无多余。

- 真题清洗稿结构：`题目数量/主试题数量` 声明必须与 manifest 的 `item_count` 一致；综合知识须有连续 `第N题` 标题、等量 `**正确答案：` 和 4×题量个 A–D 选项；案例分析/论文的主试题标题须唯一升序且数量一致。

- manifest（schema 4）：顶层仅有 `schema_version`、`updated_at`、`source_catalog`、`exams`；每卷 `notes` 非空且与两份索引的说明列逐字一致（总索引中同名冲突卷的说明带 `**同名冲突，禁止自动混并。**` 前缀）。

- 工具链：`pyproject.toml` 的 `requires-python` = `>=3.13`、`project.dependencies` 必须为空、`tool.uv.package` = `false`、`dependency-groups` 必须恰好是 `lint` 一组且只含一条 `==` 精确 pin（ruff）；`uv.lock` 的 `requires-python` 也须为 `>=3.13`。改任一处都要跑 `uv lock` 并提交锁文件。

- 全部 Markdown 必须是**标准 UTF-8（无 BOM）+ CRLF**，不得含裸 CR；校验器逐字节检查。每个存放正文的目录（含教材的每个章目录）都要有 `INDEX.md`，不得超过 200 行，且必须收录本目录下全部 Markdown 与各子目录的 `INDEX.md`；清洗目录内标题不得跳级；三个清洗目录**递归**扫描 `兰亭图书阁`、`Outer Jion` 等高风险 OCR 残留词。
- **路径命名不得含小数点**：文件名与目录名中除扩展名分隔符外不得出现 `.`，序号与标题一律用 `-` 连接（`02-历年真题-清洗版/`，不是 `02.历年真题-清洗版/`）。只有 `.github/`、`.gitignore`、`.gitattributes` 这类以点开头的约定名例外。校验器逐条扫描已跟踪路径。



## 懒加载与阅读约定



- 先读 `content/INDEX.md` 定位语料，再读该语料的 `INDEX.md`（≤ 200 行）：章节编号、标题、文件路径，可含摘要与页码范围。**先读 INDEX，再按需打开单章**，不要一次性载入全部章节。

- 大纲正文是独立单元，文件名前缀按原书阅读顺序排列；不得把清洗稿拆分形成的编号误当作原书连续章节。当前结构为：前言 → 卷首文件材料 → 一、考试说明 → 二、考试范围（科目 1、2、3 各自成篇）→ 三、题型举例（独立成篇）。**教材的每一章是一个目录** `第XX章-标题/`：目录里的 `INDEX.md` 是该章入口（frontmatter `kind: "chapter"`，含章导语与本章各节一览），正文按原书的“节”拆成 `第NN节-标题.md`（`kind: "section"`），每节一个文件。读教材时**先读章 `INDEX.md` 定位到节，再打开单节文件**，不要整章载入。

- 节文件的标题层级整体上提一级：原书的 `## N.M` 成为节文件的 `#`，其下的 `###`/`####` 相应变为 `##`/`###`，正文逐字未改。

- 每文件在 frontmatter 之后带章节标题层级，保留原文标题/表格/列表结构。
- 目录与文件名用 `-` 分段，名称内不出现小数点（见[关键不变量](#关键不变量改内容必须同步更新)）。

- 跨章问题分别读取相关章节后综合回答。

- 需要按主题、年份或科目批量筛选时，先读 frontmatter 标签，不要靠猜文件名。



## 真题加载规则



- 查真题先读 `content/02-历年真题总索引.md`（自动化读 `catalog/exams.json`），只用索引标注的“首选文件”；36 个首选均为清洗目录标准名文件。

- `sources/02-历年真题` 与 `sources/02-历年真题(补充)` 中的同名文件内容可能不同：**17 组同名冲突禁止随机选择、自动拼接、覆盖或去重合并**，差异只能人工逐项吸收。

- 2023 下综合知识有 B75（首选）与 A65（独立备选）两套题序，同样禁止拼接。

- 回忆版、公开解析版、原始整理版都不是官方标准答案；来源冲突时并列保留多版本，不得自行裁决或拼接。



## 内容治理红线



以下规则的完整文本见本文件末尾的 **RULES 规则与标准** 章节，编辑相关内容时必须遵循：



- **教材清洗/校对**：遵循 [教材 Markdown 清洗与校对标准](#教材-markdown-清洗与校对标准)——忠于原文、先纠错后美化、可读优先。

- **大纲/教材事实核查**：唯一标准是清洗稿忠于扫描件图像；核查不得使用 OCR、PDF 文本层抽取或外部事实判断。逐项比对并由另一 AI 在不看首轮结论的前提下独立验证；证据不足标“无法判定”，不得猜测。操作与记录要求见 [`verification/README.md`](verification/README.md)。

- **图表/公式**：遵循 [图表与公式处理](#图表与公式处理)——**手中没有原图就不要画图**；保留图题、清除图内 OCR 噪声，不得依据 OCR 标签串或领域常识"还原"示意图。

- **不造假**：无法确认的题面、选项、空号、连线、答案不得凭空补写，也不得写成已经确认；宁可缺失，不可编造。

- **正文只放正文**：清洗稿正文只保留题面/课文、图表题注、共用题干、作答规则与题量声明；过程记录、来源清单、核验结论不写进正文，改放 [verification/](verification/)。正文层的元数据只能以 frontmatter 形式出现，不要在正文段落里另写一份数量或来源说明。

- **数字只有一处真相**：份数、章节数、冲突组数一律写进 `catalog/corpora.json`，正文和 README 只做人类可读的转述；校验器以清单为准。

- **权利边界**：教材/大纲/真题/答案及其 OCR/清洗派生文本均为第三方内容，仓库不主张版权；仅 `scripts/` 与 `.github/workflows/` 适用 [LICENSE-CODE](LICENSE-CODE)（MIT）。边界与来源见 [内容与权利政策](#内容与权利政策)、[DATA_SOURCES.md](DATA_SOURCES.md)。不得提交盗版 PDF、下载链接或访问凭据。



## 提交与协作



- 流程见 [贡献流程](#贡献流程)：改动 → 更新来源与清单 → 跑 `uv run python scripts/validate_knowledge_base.py` → 经 PR 合并；**不要直接推送受保护的 `main`**。commit message 中不要添加任何 AI 工具署名或 `Co-Authored-By` 之类的尾注。PR 用 `.github/pull_request_template.md`，勘误/下架走 `.github/ISSUE_TEMPLATE/`。

- 换行符：`.md` 用 **CRLF**，`.py`/`.json`/`.yml`/`.yaml`/`.toml`/`uv.lock` 用 **LF**（见 `.gitattributes`、`.editorconfig`）；**所有文本文件统一为标准 UTF-8，不加 BOM**。

- 不入库：源 PDF 与扫描图（`*.pdf`、`sources/**` 下的图片格式，本地文件放在 `sources/00-…`、`sources/01-…`，只登记书目信息与校验值）、渲染/提取草稿（`tmp/`）、虚拟环境（`.venv/`）、`.claude/`。**`uv.lock` 必须入库。**



# RULES 规则与标准



以下四份规则原为独立文件（`教材Markdown清洗与校对标准.md`、`FIGURE_POLICY.md`、`CONTENT_POLICY.md`、`CONTRIBUTING.md`），2026-09 起收编为本章节，不再单独维护。各小节沿用原文标题；“本文件/本标准/本政策”等措辞按原文保留，指代对应小节自身。



## 教材 Markdown 清洗与校对标准



### 1. 适用范围



本标准适用于本仓库内由 PDF、扫描件或 OCR 提取而来的教材类 Markdown 文档，尤其适用于：



- `content/01-系统架构设计师教材-清洗版`

- `content/00-系统架构设计师考试大纲-清洗版`



两者的源扫描件登记在 `sources/01-系统架构设计师教材`、`sources/00-系统架构设计师考试大纲`。



如无特殊说明，后续对“清洗版”目录进行整理、补修、复核、返工时，均应以本标准为准。



### 2. 目标



清洗后的 Markdown 应达到以下目标：



- 保留原书信息，不改变原意，不擅自改写内容。

- 结构清晰，可按章节直接阅读。

- 去除明显 OCR 噪声、页眉页脚、出版宣传等非正文内容。

- 修复因 OCR 或早期清洗造成的断句、标题误判、列表缺失、图文串行等问题。

- 在“内容完整性”优先于“格式好看”的前提下，尽量提升可读性。



### 3. 基本原则



#### 3.1 忠于原文



- 以 `sources/` 中登记的源扫描件或原始教材内容为准。

- 允许修正 OCR 错字、断句、粘连、误判标题。

- 不允许为了“润色”而改写作者原意。

- 不允许在没有依据的情况下补充新知识或扩写解释。



#### 3.2 先纠错，后美化



- 优先修复信息缺失、结构错误、标题误判、列表断裂。

- 格式统一是必要工作，但不能压过内容正确性。



#### 3.3 可读优先



- 表格无法完整恢复时，允许改写为条目列表，但必须保留关键信息。

- 图中文字若混入正文且影响阅读，应删除图内噪声，只保留图题和正文说明。



### 4. 清洗规则



#### 4.1 文件与标题层级



- 大纲正文文件名格式统一为 `NN-标题.md`，`NN` 只表示原书阅读顺序，不代表原书章号。教材每章是目录 `第XX章-标题/`，章入口为其中的 `INDEX.md`，各节为 `第NN节-标题.md`。

- 每个章节文件开头必须有明确的章节标题。

- 一级、二级、三级标题优先保留教材原有层级关系。

- 不得把正文句子误清洗成标题。

- 不得把“第 X.X.X 节已经提到”“如图 X-X 所示”之类正文误判为标题。

- 章节内枚举型小节，如“1. 时间分配”“2. 选择题目”，应与所在层级保持一致，不随意提升或降级。



建议层级：



- `#` 或 `##`：章节主标题

- `##` 或 `###`：节标题，如 `12.3`

- `###` 或 `####`：小节标题，如 `12.3.1`

- `####` 或 `#####`：节内条目式小标题



核心要求不是机械统一层级深度，而是层级关系必须正确、可读。



#### 4.2 段落与断行



- 被 OCR 错误切断的同一自然段应合并。

- 被错误粘连的两个段落应拆开。

- 单独成行但语义不完整的残句，一般应并回上下文。

- 避免保留明显由换页造成的半句、断词、断段。



#### 4.3 列表与题干结构



- 题目、案例、步骤、优缺点、条件、要点等内容，若原文本应为列表，清洗后必须恢复为列表。

- 不得将完整编号列表压扁成一个段落。

- 不得将列表项误识别为新章节标题。

- 论文题干、案例分析、问答题等结构，应恢复为：

  - 题目

  - 背景说明

  - 分问列表

  - 分析要点



#### 4.4 表格处理



- 能可靠恢复表头与列结构时，优先恢复为 Markdown 表格。

- 不能可靠恢复时，可改写为“表题 + 条目列表”。

- 表格内容宁可转为可读条目，也不要保留一整行不可读的拼栏文本。

- 表格中的图例、页码、装饰符应删除。



#### 4.5 图片与图题



- 保留图题，如 `> 图 12-1 xxx`。

- 删除图内被 OCR 识别出来且混入正文的乱码、标签串、组件框文字堆叠。

- 若正文中引用图示，应保留“如图 X-X 所示”之类说明。

- 图题不得重复出现两次。



#### 4.6 OCR 噪声清除



必须删除的典型噪声包括：



- 页眉页脚

- 页码

- 重复章名

- 出版社宣传语

- 定价、ISBN、版次、印次、责任编辑等出版信息

- “图书查询”“扩展阅读”“扫码”“公众号”等营销或导流信息

- 水印类文本，如 `兰亭图书阁`

- 图内散落标签串

- 孤立的 `>`、`---`、无意义数字行、乱码行



#### 4.7 OCR 错字与术语修正



- 对明显的 OCR 错字应纠正，如：

  - “架架构设计师”改为“架构设计师”

  - “802. 11 b”合并为 `802.11b`

  - “Outer Jion”若原文如此可保留，若确定为 OCR 错误可改为 `Outer Join`

- 专业术语、英文缩写、型号、协议名应尽量与上下文保持一致。

- 对无法确认的专有名词，不要主观改写。



#### 4.8 中英文与标点



- 中文正文使用中文标点为主。

- 英文术语、协议名、标准号、缩写按技术写法保留。

- 中英文之间保持基本可读空格，不必过度机械化。

- 避免出现明显的空格错位、括号残缺、引号不闭合。



### 5. 校对规则



校对不是只看格式，而是按以下顺序检查：



#### 5.1 内容完整性



- 是否有关键句、关键列表项、关键步骤丢失。

- 是否有题干、案例、时间分配、优缺点等被截断。

- 是否有换页导致的半句未补回。



#### 5.2 结构正确性



- 标题层级是否正确。

- 正文是否被误识别成标题。

- 列表是否被打散。

- 图题、表题、案例结构是否完整。



#### 5.3 可读性



- 段落是否通顺。

- 表格或图示说明是否至少达到“可理解”。

- 是否还存在大段拼栏文本。



#### 5.4 噪声残留



- 是否仍残留水印、页眉页脚、出版宣传、扫码导流信息。

- 是否仍存在孤立乱码行、重复图题、无意义符号行。



#### 5.5 一致性



- 文件标题、章节编号、`INDEX.md` 是否一致。

- 同类术语写法是否前后一致。

- 同类章节的结构风格是否大体统一。



### 6. 推荐工作流



#### 6.1 开始前



- 先读对应目录的 `INDEX.md`。

- 明确本次处理范围，不一次性无差别读取全部章节。

- 如已有样板章节，优先对齐样板水准。



#### 6.2 单章处理流程



1. 阅读清洗版当前文件。

2. 对照 `sources/` 中登记的源扫描件对应页（教材章节的印刷页范围写在该文件的 `source_pages` 标签里）。

3. 先修内容缺失和结构错误。

4. 再修断句、列表、图表题注。

5. 最后清理噪声和统一格式。



#### 6.3 单章完成后检查



至少执行以下检查：



- 检查是否存在明显营销/出版残留。

- 检查是否存在伪标题。

- 检查是否存在孤立残句或乱码行。

- 抽读高噪声区域，确认语义连贯。



### 7. 推荐检索关键词



清洗后建议检索以下高风险残留词：



- `兰亭图书阁`

- `图书查询`

- `扩展阅读`

- `定价：`

- `清华大学出版社`

- `扫码`

- `公众号`

- `责任编辑`

- `版次`

- `印张`



也建议检索以下结构异常特征：



- 形如“正文句子被写成标题”的行

- 孤立的 `>`

- 成片的 `●`

- 协议名/标准号被切碎的行

- 表格被压成一整行的文本



### 8. 禁止事项



- 不得擅自删除有实际信息价值的正文。

- 不得为了省事保留明显不可读的大段拼栏文本。

- 不得将原书案例、题干、步骤随意改写成自己的表达。

- 不得只做格式清理而放任内容丢失。

- 不得在没有比对原文的情况下大范围“想当然修正”。



### 9. 交付标准



一份合格的清洗版 Markdown，至少应满足：



- 章节结构正确。

- 正文连续可读。

- 关键内容无明显缺失。

- 常见 OCR 噪声已清除。

- 图表题注可识别。

- 不影响后续 AI 检索、学习和引用。



### 10. 备注



当“格式更漂亮”和“信息更完整”发生冲突时，优先选择“信息更完整”。



当“完全恢复原版排版”成本过高时，允许退而求其次转为清晰的 Markdown 列表或段落，但不得牺牲内容正确性。



## 图表与公式处理



本仓库以纯文本 Markdown 为主，原始教材和真题中的图片未随仓库收录（`content/` 下只有文本，`assets/` 仍为空）。**图题存在不代表原图可用。**



### 核心规则：没有原图就不要画图



只有**手中有该图的原始图像并在制作时实际对照**，才能把图转成 SVG、Mermaid 或表格。仅凭 OCR 残留的标签串、上下文散文或领域常识"还原"出来的图，是生成内容而非原图，一律禁止。



历史教训：2026-09 之前仓库曾有 325 个由 AI 生成的 Mermaid 示意图。比对原始 OCR 后，其节点文字总命中率约 53%；即使节点文字全部命中的图，连线方向与拓扑在原文中也毫无依据（如教材图 2-3，9 个节点全部有出处，14 条连线全部是生成的）。这些图已全部移除，位置登记在 [verification/待重画图表清单.md](verification/待重画图表清单.md)。



### 有原图时的等价要求



1. 节点数量、节点文字、连线方向、层次归属与原图逐一一致；

2. 不补画原图没有的元素（原图没有箭头就不画箭头）；

3. 不美化、不合并同类项、不推测看不清的部分；看不清就留空并在图题或 `<desc>` 中写明；

4. 版式差异（列宽、字体、绝对尺寸）无需复刻，但承载信息的几何关系必须保留。



### 形式选择



按原图性质决定，不自由选择：



| 原图性质 | 形式 |

|---|---|

| 行列对应表 | Markdown 表格 |

| 流程、时序、状态机、类图、树形层次 | Mermaid |

| 几何本身承载信息：跨边界、重叠、嵌套包含、带编号部件的物理布局 | 独立 `.svg` 文件 |

| 纯装饰、原书正文未引用 | 不转，直接删除，不留占位 |



SVG 必须作为独立文件落盘并以 Markdown 图片语法引用——**GitHub 会剥掉内联 `<svg>`，内联图在网页上不显示**。SVG 需自身格式良好、无 BOM，并带 `<title>`/`<desc>` 无障碍标注。



### 其他



- 保留图题（如 `> 图 12-1 xxx`）与正文中"如图 X-X 所示"之类的引用说明，删除图内被 OCR 混入的标签串、坐标碎片和乱码。

- 公式优先用 KaTeX（`$`/`$$`）表达；无法可靠恢复的公式保留原文文字描述，不臆造推导。

- 如确有权纳入原始图片，放入 `assets/figures/<语料 id>/<章节 slug>/`（例：`assets/figures/textbook/ch07-architecture-design-fundamentals/figure-7-3.svg`），使用原图号命名并移除无关水印或个人信息；命名细则见 [assets/README.md](assets/README.md)，大批量图片引入前评估 Git LFS 和再分发权利。



## 内容与权利政策



### 1. 适用范围与项目定位



本政策适用于本仓库中的教材、考试大纲、历年试题、答案、解析、OCR 文本、清洗稿、图片、脚本、工作流和项目文档。



本仓库是非官方、非商业的个人学习资料整理项目，与考试主管机构、原作者、出版社以及被引用的网站不存在隶属、合作或授权关系。资料被收录、OCR、清洗或重新排版，不表示其权利状态发生变化，也不表示维护者取得或主张第三方内容的著作权。



### 2. 权利边界



#### 2.1 第三方内容



下列内容可能受第三方著作权、商标权、数据库权利或其他权利保护：



- `sources/00-系统架构设计师考试大纲/` 登记的考试大纲与 `content/00-系统架构设计师考试大纲-清洗版/` 中的 OCR/清洗派生文本；

- `sources/01-系统架构设计师教材/` 登记的教材与 `content/01-系统架构设计师教材-清洗版/` 中的 OCR/清洗派生文本；

- `sources/02-历年真题/`、`sources/02-历年真题(补充)/` 与 `content/02-历年真题-清洗版/` 中的试题、题面、答案、解析和公开回忆版内容；

- 从出版物或外部网站恢复的图片、表格、版式、引用和其他素材。



相关权利归各自权利人所有。本仓库不对上述内容授予开源许可，也不保证使用者因访问本仓库即获得复制、传播、改编、训练模型或商业使用的权利。使用者应自行确认其使用行为符合适用法律、来源网站条款和权利人的许可条件。



“仅供学习”“注明来源”或“经过 OCR/清洗”本身不当然构成授权，也不免除使用者的合规责任。



#### 2.2 项目自有代码



只有由本项目贡献者原创并提交在 `scripts/` 和 `.github/workflows/` 中的脚本、检查器及 GitHub Actions 工作流，适用 [LICENSE-CODE](LICENSE-CODE) 所载 MIT License。



该代码许可明确不覆盖教材、大纲、试题、答案、解析、OCR/清洗文本、图片、表格、出版物版式、来源网站内容或其他第三方素材。文件与代码位于同一仓库，不表示它们采用相同许可。



### 3. 内容使用与质量声明



- 仓库内容仅作为学习、检索和校对辅助，不构成官方教材、官方题库、官方答案或考试通知。

- OCR 和人工整理可能造成错字、缺页、断句、图表丢失、题序变化或答案误判。

- 回忆版试题及解析可能彼此冲突；重要场景应回到权威来源核对。

- 不得为追求“完整”而凭空补写第三方原文、图表、题面或答案；无法确认的内容保持缺失，不编造。



### 4. 新增与修改内容的来源要求



提交第三方内容或修订现有资料时，贡献者应：



1. 确认自己有权提交，或将提交控制在适用法律及来源许可允许的范围内；

2. 在 [DATA_SOURCES.md](DATA_SOURCES.md) 中记录来源名称、可用链接或出版信息、版本及派生关系；

3. 区分原文与推断，不把回忆版标成官方发布；

4. 保留必要署名和来源说明，不移除权利声明或技术保护信息；

5. 无法确认来源的题面、答案或图表不要补写，保持缺失即可。



请勿提交盗版文件下载链接、访问凭据、绕过付费或技术保护的说明，或无权公开的源 PDF/扫描件。



### 5. 勘误流程



发现 OCR、结构、来源或答案问题时，优先在仓库 [GitHub Issues](https://github.com/YoungHong1992/ruankao-senior-architecture-designer/issues) 提交，标题建议使用 `【勘误】文件名：问题摘要`，并提供：



- 文件路径、章节/题号和可定位的上下文；

- 当前内容与建议修改；

- 可复核证据，例如合法来源中的页码、出版信息或公开链接；

- 问题类型：OCR 错误、清洗遗漏、原资料疑误、来源缺失、答案争议或图表缺失。



维护者应优先作最小、可验证的修正；证据不足时保留争议标记，不把推测写成确定结论。若错误来自原出版物或回忆来源，应明确标为“原资料如此”或“来源间存在差异”，避免静默改写。



### 6. 权利人联系与下架流程



权利人或其授权代表如认为仓库内容侵犯其权利，可通过 [GitHub Issues](https://github.com/YoungHong1992/ruankao-senior-architecture-designer/issues) 提交标题为 `【版权/下架请求】内容标识` 的请求；如材料包含个人信息、合同或其他敏感证据，请不要公开上传，请改用仓库的私密上报渠道（[Security 页面](https://github.com/YoungHong1992/ruankao-senior-architecture-designer/security) → **Report a vulnerability**），该会话仅维护者可见，具体说明见 [SECURITY.md](SECURITY.md)。



请求宜包含：



- 请求人的姓名或机构、与权利人的关系及可联系信息；

- 被主张权利的作品或资料；

- 需要处理的仓库文件路径或精确 URL；

- 权利依据及请求的处理方式，例如补充署名、纠正来源、限制展示或移除；

- 请求人确认所提供信息真实、且有权代表相关权利人提出请求。



维护者收到信息充分的请求后，将核对涉及范围，并可在核验期间先行移除或限制有争议内容；确认后根据情况删除、替换、补充署名或恢复。为保护各方隐私，维护者可要求后续通过非公开渠道补充必要证据，但不会要求在公开 Issue 中披露身份证件等敏感信息。



本流程用于及时处理内容争议，不构成对任何一方权利或责任的预先认定，也不是法律意见。



## 贡献流程



感谢帮助校对知识库。提交前请先阅读上文「项目规范」全部内容，特别是[内容治理红线](#内容治理红线)、[教材 Markdown 清洗与校对标准](#教材-markdown-清洗与校对标准)、[内容与权利政策](#内容与权利政策)和[图表与公式处理](#图表与公式处理)。



### 可接受的贡献



- 有原书页码或可靠来源支撑的 OCR 勘误；

- 标题、列表、表格、代码块和题号结构修复；

- 真题来源、题量、版本差异及答案争议的可追溯补录；

- 索引、验证脚本和工作流改进；

- 在有权提交的前提下恢复必要图表。



不要提交未获授权的源 PDF/扫描件、盗版下载链接、访问凭据、付费内容绕过方法，或把 AI 推测内容标成原文、原图、官方题面或官方答案。



### 内容修改流程



0. 安装 [uv](https://docs.astral.sh/uv/getting-started/installation/)（macOS/Linux：`curl -LsSf https://astral.sh/uv/install.sh | sh`；Windows PowerShell：`powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"`）。本仓库脚本的解释器（Python 3.13）与依赖均由 uv 管理，一律用 `uv run ...` 执行，不要直接调用系统 `python`；首次运行会自动准备虚拟环境，无需手动 `pip install`。

1. 先读 `content/INDEX.md`，再读目标语料的 `INDEX.md`；真题先读 `content/02-历年真题总索引.md`。

2. 教材清洗应同时对照 `sources/` 登记的源扫描件和依法取得的原资料，优先做最小、可验证的修正。

3. 在 `DATA_SOURCES.md` 中记录来源、版本和派生关系；历史 URL 不可追溯时，不得猜测。

4. 来源冲突时保留多个版本，说明差异，不得直接拼接。

5. 跑完 CI 会跑的**全部检查**，全绿再提交：



   ```bash

   uv lock --check                                          # pyproject.toml 与 uv.lock 一致

   uv run python scripts/validate_knowledge_base.py         # 主门禁，应输出 PASSED: 0 errors

   uv run --group lint ruff check scripts/                   # 仅在改了 scripts/ 时需要

   uv run --group lint ruff format scripts/                  # 同上；CI 用 --check 断言已格式化

   ```



   CI 用的是 `uv run --locked`，本地加 `--locked` 可以完全对齐（改过 `pyproject.toml` 但没跑 `uv lock` 时它会直接报错，而不是静默改写锁文件）。

6. 通过 Pull Request 合并；不要直接推送受保护的 `main`。



### 修改脚本依赖



- `validate_knowledge_base.py` 只能使用 Python 标准库，以保证质量门禁不依赖任何第三方包。

- 代码风格由 ruff 统一（配置在 `pyproject.toml` 的 `[tool.ruff]`，行宽 120），ruff 本身固定在非默认的 `lint` 依赖组，因此主门禁在无第三方包的环境里也必须通过。

- 改动 `pyproject.toml` 后运行 `uv lock` 并把更新后的 `uv.lock` 一并提交。



### Pull Request 要求



- 标题说明资料、章节/年份和修改类型；

- 正文列出修改文件、证据、已知限制和验证结果；

- 不混入无关格式化或大规模改写；

- 内容修改优先由另一名维护者复核；单人维护且暂无独立复核人时，可由提交者完成逐项自查，并在来源、已知限制和验证结果记录完整且自动检查通过后合并。



项目自有脚本和工作流采用 [LICENSE-CODE](LICENSE-CODE)；提交第三方内容不表示其采用该许可证。提交者应确认自己有权提交相关变更。

