# 图表与公式资产策略

本仓库当前以纯文本 Markdown 为主，原始教材和真题中的图片资产尚未随仓库收录。图题存在不代表原图可用；任何依赖原图、原表或公式排版的信息，都必须显式说明完整度。

## 处理等级

### A. 缺图会改变题意或知识内容

适用于真题表格、流程图、时序图、公式推导、带编号部件的架构图等。不得根据上下文臆造原图内容，也不得把推测答案写成已确认答案。

统一标记：

> **图表状态：** 原图未收录；本段信息不完整，需回查原书或可靠来源。

### B. 正文可独立理解

保留图题和正文解释，删除图内被 OCR 混入的标签串。

统一标记：

> **图表状态：** 原图未收录；仅保留图题与正文说明，不能据此还原原图细节。

### C. 纯装饰或无信息价值图片

可删除图片 OCR 噪声；若原书正文没有引用该图，可不保留占位。

## 后续恢复与重绘方式

1. 取得可复核来源后，可以把图表转为 Mermaid、Markdown 表格或文字关系表；必须保留原图可确认的节点、空号、方向和层次，不补写无法唯一判定的几何或答案。
2. 在相邻 Markdown 中记录资料版次、原书页码，或公开页面 URL、固定提交/文件标识、访问日期及 SHA-256；不同来源有冲突时并列记录，不静默裁决。
3. 人工或 AI 辅助重绘只能标为“结构化重绘/学习示意”，不得冒充原图。所有 Mermaid 和复杂表格应实际渲染并目视检查可读性、裁切和连线方向。
4. 只有在结构已由来源支持、恢复说明完整且残余限制明确后，才删除相应的旧缺图标记；无法唯一恢复的部分继续保留为显式限制项。
5. 如确有权纳入原始图片，应放入 `assets/figures/<资料>/<章节>/`，使用原图号命名并移除无关水印或个人信息；大批量图片引入前评估 Git LFS 和再分发权利。
6. 内容修改优先由另一名维护者复核；单人维护时应完成逐项来源核对、渲染检查和自动验证，并记录验证结果。
7. 删除旧缺图标记不等于资产恢复完成。教材修改后应使用 `uv run --group audit python scripts/audit_textbook_pdf.py` 对本地合法持有的 PDF 重新生成 `data/textbook_audit.json`，核对 PDF 图号/表号与 Markdown 独立载体；真题点位变化应同步 `data/exam_asset_audit.json`。两个账本都必须保留来源受限项，不能为了让计数归零而删除限制说明。

## 真题特别规则

- 缺少题目所依赖的图或表时，试卷完整度不得标为“完整”。
- 无法核对原图时，答案可信度最高只能标为“低”或“待复核”。
- 不同回忆版对图表内容有冲突时，分别保留来源，不得合并成未经证实的“完整题面”。
